<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.ddtkj.grabRedEnvelopeModule"
    xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".Base.Application.debug.GrabRedEnvelopeModule_Application"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/icon_launcher"
        tools:replace="name,icon,label,theme,allowBackup"
        android:supportsRtl="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning"
        android:theme="@style/AppTheme">

        <!--主Activity-->
        <activity
            android:name=".MVP.View.Implement.Activity.GrabRedEnvelopeModule_Act_MainActivity_View"
            android:theme="@style/AppDefaultStyle"
            android:launchMode="singleTask"
        >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>